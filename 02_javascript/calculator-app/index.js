const { calculate } = require('./services/calculator');

try {
// TODO: Reemplaza estas pruebas por las tuyas a medida que avances
console.log('Result:', calculate(5, 2, '*'));
} catch (err) {
console.error('Error:', err.message);
}

