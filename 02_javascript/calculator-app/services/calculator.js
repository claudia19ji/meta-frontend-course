const { add, subtract, multiply, divide } = require('../utils/math');


// Orquestador de operaciones: NO hace cálculos directos, solo delega a utils
function calculate(a, b, operator) {
// TODO: validaciones defensivas → typeof a/b === 'number'
// TODO: switch o if/else para seleccionar operación entre '+', '-', '*', '/'
// TODO: retornar el resultado de la util correspondiente
}


module.exports = { calculate };

 